# Lane 1 report — claim Tiny Studio as an affiliated Organization

Item: d4be819830
Branch: `lane1/claim-studio-affiliation-20260822`
Base: origin/main `63baab9` (`daily: publish 2026-08-22`)

## Why this is not already resolved

On current origin/main the homepage Person already lists Tiny Studio in
`sameAs` (PR #94) and carries `hasOccupation` (PR #115). The footer already
links `https://tinystudio.in/` with `rel="me noopener noreferrer"`. There is
still no top-level `Organization` node and no `Person.affiliation` edge, so
machines see a URL identity, not the studio-as-organization relationship the
footer already shows.

## Change

- `scripts/build_daily.py` `json_ld()`: add a Tiny Studio Organization node
  (`@id` `https://inish.in/#studio`, `url` `https://tinystudio.in/`) as the
  last `@graph` element, and point `Person.affiliation` at that `@id`.
  Property is `affiliation`, not `worksFor`. Organization fields are only
  `@id`, `@type`, `name`, and `url`. `sameAs` order is unchanged.
- `tests/test_build_daily.py`: pin the new node type, Organization fields,
  and the affiliation reference.
- `index.html`: regenerated by `python3 scripts/build_daily.py`. JSON-LD
  was not hand-edited. Footer markup is unchanged.

## Verification

1. `python3 scripts/build_daily.py` → `built latest=2026-08-22 stories=6 scanned=247`
2. `python3 -m unittest discover -s tests -v` → 120 tests, `OK`
3. Manual JSON-LD check on committed `index.html`: one Organization
   `{@id: https://inish.in/#studio, @type: Organization, name: Tiny Studio,
   url: https://tinystudio.in/}` and Person.affiliation `{@id: https://inish.in/#studio}`
4. `latest.json`, `feed.xml`, and `sitemap.xml` were not rewritten.

Footer remains: `rel="me noopener noreferrer"` and `Tiny Studio ↗ — Nish's studio.`
